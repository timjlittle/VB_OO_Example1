Module Globals
    '* This is a kludge, try not to hard code paths
    '* A better solution is to use the registry 
    '* and force the user into a setup screen the
    '* first time the app is run
    Public ExcelPath As String = "c:\users\TJL-Dell\Company.xlsx"
End Module
